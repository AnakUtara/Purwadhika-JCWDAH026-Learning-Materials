{
	"name": "product-management-dashboard",
	"version": "1.0.0",
	"private": true,
	"description": "Root folder for the Product Management Dashboard project.",
	"scripts": {
		"dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\""
	},
	"devDependencies": {
		"concurrently": "^9.2.3"
	}
}
